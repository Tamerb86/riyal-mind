# 🚀 ريال مايند - Riyal Mind (الإصدار المحسّن)

تطبيق ويب متكامل لإدارة المصاريف العائلية والشخصية، مصمم خصيصاً للسوق السعودي. يساعدك على تتبع نفقاتك، إدارة الاشتراكات، ومشاركة المصاريف مع العائلة أو الأصدقاء بكل سهولة وأمان.

---

## ✨ المميزات الرئيسية

| الميزة | الحالة | ملاحظات |
| :--- | :--- | :--- |
| **🏠 لوحة تحكم رئيسية** | ✅ **مكتمل** | نظرة شاملة على الوضع المالي. |
| **💸 إدارة المصاريف** | ✅ **مكتمل** | إضافة، تعديل، وحذف المصاريف مع تصنيفات. |
| **💳 نظام الاشتراكات (Stripe)** | ✅ **مكتمل** | 4 خطط دفع (فردي/عائلي، شهري/سنوي) مع خصم 25%. |
| **👨‍👩‍👧‍👦 الحساب العائلي/المشترك** | ✅ **مكتمل** | إنشاء مجموعات، دعوة أعضاء، وتقسيم المصروفات. |
| **📊 التقارير المالية** | ✅ **مكتمل** | تقارير مبسطة لتتبع الإنفاق. |
| **🔐 المصادقة والأمان** | ✅ **مكتمل** | تسجيل الدخول عبر البريد الإلكتروني/كلمة المرور أو Google. |
| **📄 الصفحات القانونية** | ✅ **مكتمل** | سياسة الخصوصية وشروط الاستخدام متوافقة مع PDPL و GDPR. |
| **📞 صفحة اتصل بنا** | ✅ **مكتمل** | نموذج تواصل احترافي. |
| ** landing Page المحسّنة** | ✅ **مكتمل** | صفحة هبوط احترافية مع أقسام متعددة (How it works, FAQ, etc.). |

---

## 🛠️ التقنيات المستخدمة

- **Framework**: Next.js 14 (App Router)
- **Language**: TypeScript
- **Database**: PostgreSQL (مستهدف لـ Supabase) + Prisma ORM
- **Authentication**: NextAuth.js v5
- **Payments**: Stripe
- **UI**: Tailwind CSS + shadcn/ui + Radix UI
- **Email**: Resend
- **Validation**: Zod

---

## 🚀 دليل التثبيت والتشغيل

### 1. المتطلبات الأساسية

- Node.js (v18 أو أحدث)
- npm أو pnpm
- حساب Supabase (للحصول على رابط قاعدة البيانات)
- حساب Stripe (للحصول على مفاتيح API)

### 2. إعداد المشروع

1.  **تثبيت المكتبات:**

    ```bash
    npm install
    ```

2.  **إعداد متغيرات البيئة:**

    - انسخ ملف `.env.example` إلى ملف جديد باسم `.env.local`.
    - املأ جميع المتغيرات المطلوبة، خاصة:
      - `DATABASE_URL`: رابط الاتصال بقاعدة بيانات Supabase.
      - `STRIPE_*`: جميع مفاتيح Stripe السرية والعامة.
      - `NEXTAUTH_SECRET`: مفتاح سري فريد.

3.  **تجهيز قاعدة البيانات (أول مرة فقط):**

    - **توليد Prisma Client:**

      ```bash
      npx prisma generate
      ```

    - **تطبيق الـ Migrations على قاعدة بيانات Supabase:**

      ```bash
      npx prisma migrate deploy
      ```

4.  **تشغيل المشروع محلياً:**

    ```bash
    npm run dev
    ```

### 3. دليل النشر (Deployment)

- **الواجهة الأمامية (Frontend):** يمكن نشرها بسهولة على **Vercel** عن طريق ربط حساب Vercel بحساب GitHub الخاص بك.
- **قاعدة البيانات (Backend):** استخدم **Supabase** كقاعدة بيانات PostgreSQL.

**خطوات النشر:**

1.  ارفع الكود إلى مستودع GitHub.
2.  في Vercel، قم بإنشاء مشروع جديد واربطه بالمستودع.
3.  أضف جميع متغيرات البيئة من ملف `.env.local` إلى إعدادات المشروع في Vercel.
4.  قم بتشغيل عملية النشر (Deploy).

---

## 📝 ملاحظات هامة

- **تحديث قاعدة البيانات:** عند إجراء أي تغييرات على `prisma/schema.prisma` في المستقبل، يجب تشغيل `npx prisma migrate dev --name your_migration_name` لإنشاء migration جديد، ثم `npx prisma migrate deploy` لتطبيقه على قاعدة البيانات.
- **البريد الإلكتروني:** تم توحيد جميع رسائل البريد الإلكتروني لتصدر من `support@riyalmind.com`.
